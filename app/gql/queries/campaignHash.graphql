query CampaignHash($input: CampaignHashInput!) {
  campaignHash(input: $input) {
    name
    description
    hash
    advertiser {
      name
    }
    criteria {
      placements {
        publisher {
          id
          name
          logo
        }
      }
    }
  }
}
