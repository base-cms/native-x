query currentUser {
  currentUser {
    id
    email
    familyName
    givenName
    photoURL
    activeAccount {
      id
      name
      photoURL
    }
    accounts {
      id
      name
      photoURL
    }
  }
}
