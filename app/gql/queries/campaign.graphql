query campaign($input: ModelIdInput!) {
  campaign(input: $input) {
    id
    name
    advertiser {
      id
      name
    }
    url
    status
    creatives {
      id
      title
      teaser
      image {
        id
        src
      }
    }
  }
}
