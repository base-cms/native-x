query campaignNotifications($input: ModelIdInput!) {
  campaign(input: $input) {
    id
    notify {
      internal {
        id
        name
        email
      }
      external {
        id
        name
        email
      }
    }
  }
}
