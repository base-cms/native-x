mutation AssignCampaignExternalContact($input: RemoveCampaignExternalContactInput!) {
  removeCampaignExternalContact(input: $input) {
    id
    notify {
      external {
        id
        name
        email
      }
    }
  }
}
