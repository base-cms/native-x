<div class="row">
  <div class="col">
    <h3 class="mb-0">
      Publishers
    </h3>
  </div>
</div>

{{#paginate-models
  models=model
  routeName='publisher.index'
  totalCount=totalCount
  hasNextPage=hasNextPage
  endCursor=endCursor
  first=first
  after=after
  ascending=ascending
  sortBy=sortBy
  sortOptions=sortOptions
  as |model|
}}
  {{partial 'publisher/list'}}
{{/paginate-models}}
