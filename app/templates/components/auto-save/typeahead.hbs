{{log 'here' class}}
{{yield (hash
  element=(component "form-elements/typeahead"
    id=id
    triggerClass=class
    tabindex=tabindex
    required=required
    placeholder=placeholder
    disabled=disabled
    aria-describedby=aria-describedby

    timeout=typeDelay
    value=value
    placeholder=placeholder
    closeOnSelect=closeOnSelect
    disabled=disabled

    validator=validator
    on-insert=(action "setChildInputComponent")
    on-search=on-search
    on-change=(action sendOnChange)
  )
  elements=(hash
    indicator=(component "auto-save/indicator"
      visible=shouldSave
      isChanging=isChanging
      changeComplete=changeComplete
      validationMessage=_child.validationMessage
    )
    invalid-feedback=(component "auto-save/invalid-feedback"
      message=_child.validationMessage
    )
  )
)}}
