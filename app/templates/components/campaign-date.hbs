{{#if date}}
  <small class="d-block">
    {{entypo-icon "calendar"}} {{label}} {{moment-from-now date}} on {{moment-format date format}}
  </small>
{{else}}
  <small class="d-block">
    {{entypo-icon "calendar"}} {{label}} Never
  </small>
{{/if}}
