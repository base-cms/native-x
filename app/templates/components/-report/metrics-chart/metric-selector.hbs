{{#power-select
  triggerClass="metric-selector"
  options=options
  selected=selected
  searchField="label"
  disabled=disabled
  onchange=(action "setMetric") as |metric|
}}
  {{metric.label}}
{{/power-select}}
