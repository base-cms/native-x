<div class="card-body d-flex flex-row justify-content-between">
  <div class="d-flex flex-column">
    <span class="h1 mb-0 my-auto {{color}}">
      {{entypo-icon icon}}
    </span>
  </div>

  <div class="d-flex flex-column text-right">
    <span class="text-uppercase">{{title}}</span>
    <span class="h4 mb-0 {{color}}" title="{{value}}">{{number-format value format}}</span>
    {{#if percentage}}
      <small class="text-muted">({{number-format percentage "0%"}})</small>
    {{/if}}
  </div>
</div>
{{#if subtitle}}
  <div class="text-center text-muted border-top py-2">
    <small class="text-muted text-uppercase">{{ subtitle }}</small>
  </div>
{{/if}}
