{{#if displayNav}}
  {{partial "navigation"}}
{{/if}}

<div class="container mt-4 mb-4">
  {{outlet}}
</div>

<div id="loading-trigger" style="display:none; opacity:0.8">
  {{partial "loading"}}
</div>

{{ember-notify messageStyle='bootstrap'}}
{{#-bs/button 'secondary' class='fixed-bottom' click=(route-action 'scrollToTop')}}{{-ion 'chevron-up'}}{{/-bs/button}}
